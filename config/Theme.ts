const colors = {
  primary: '#B9A1FC', // Color for buttons or links
  bg: '#fff', // Background color
  white: '#fff',
  grey: {
    dark: '#23047C',
    default: '#15024A',
    light: '#B59CFC',
    ultraLight: '#D3C4FD',
  },
};

const transitions = {
  normal: '0.5s',
};

const fontSize = {
  small: '0.9rem',
  big: '2.9rem',
};

export default {
  colors,
  transitions,
  fontSize,
};
